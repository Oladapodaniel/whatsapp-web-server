{
  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
    "rewrites": [
      { "source": "/socket.io/:path*", "destination": "/_socket.io/:path*" },
      { "source": "/:path*", "destination": "/:path*" }
    ]  
}